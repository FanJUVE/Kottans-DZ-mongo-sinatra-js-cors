
<h1><%= message %></h1>
<div id="weather-local" class="container">Тут будет погода с локалки</div>
<hr>

<h1><%= message2 %></h1>
<div id="weather-openweather" class="container">
  <% todays_weather.each do |weather| %>
    <div>
      <ul>
        <li><%= weather.date.strftime("%d/%m/%Y") %></li>
        <li><%= weather.city %></li>
        <li><%= weather.temp%></li>
        <li><%= weather.sky%></li>
      </ul>
    </div>
  <% end %>
</div>
<hr>